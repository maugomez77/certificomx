services:
  - type: web
    name: certificomx-api
    runtime: python
    buildCommand: pip install -e .
    startCommand: uvicorn certificomx.api:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: ANTHROPIC_API_KEY
        sync: false
